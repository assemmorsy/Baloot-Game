'use strict';

/**
 * website-visits-counter controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::website-visits-counter.website-visits-counter');
